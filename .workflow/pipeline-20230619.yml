version: '1.0'
name: pipeline-20230619
displayName: pipeline-20230619
triggers:
  trigger: auto
  push:
    branches:
      prefix:
        - ''
stages:
  - name: stage-6150a49a
    displayName: 未命名
    strategy: naturally
    trigger: auto
    executor: []
    steps:
      - step: build@nodejs
        name: build_nodejs
        displayName: Nodejs 构建
        nodeVersion: 14.16.0
        commands:
          - '# 设置NPM源，提升安装速度'
          - npm config set registry https://registry.npm.taobao.org/
          - ''
          - '# 安装pnpm'
          - corepack enable
          - corepack prepare pnpm@latest --activate
          - ''
          - '# 进行构建'
          - cd ./docs
          - pnpm docs:build
        artifacts:
          - name: BUILD_ARTIFACT
            path:
              - ./dist
        caches:
          - ~/.npm
          - ~/.yarn
        notify: []
        strategy:
          retry: '0'
